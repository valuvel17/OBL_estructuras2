#ifndef CHARS
#define CHARS

//PRE: Recibe un numero en formato string
//POS: Devuelve un int con el valor del numero 
int charAint(char* cadena);

//PRE: Recibe un string
//POS: Devuelve un Array con las palabras del string separadas por " "
char** separarChars(char* cadena);

#endif